{
  "name": "sondri-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Sondri - AI agent, Business & Coding Expert platform and marketing site",
  "scripts": {
    "dev": "npm --prefix sondri-site run dev",
    "build": "npm --prefix sondri-site run build",
    "check": "npm --prefix sondri-site run check",
    "preview": "npm --prefix sondri-site run preview"
  }
}
