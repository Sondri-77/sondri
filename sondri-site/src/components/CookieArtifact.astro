<div id="cookie-artifact" class="artifact" hidden>
  <div class="artifact-bar">
    <span class="artifact-title">COOKIES</span>
    <button data-dismiss aria-label="Dismiss cookie notice">X</button>
  </div>
  <div class="body">
    <p class="artifact-body">BY CONTINUING TO USE THIS WEBSITE, YOU AGREE TO OUR PRIVACY POLICY.</p>
    <button class="ok" data-dismiss>OK</button>
  </div>
</div>

<style>
  #cookie-artifact {
    position: fixed;
    left: 24px;
    bottom: 24px;
    z-index: 60;
    width: 300px;
    max-width: calc(100vw - 48px);
  }
  .artifact-bar button {
    cursor: pointer;
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--ink);
    background: transparent;
    border: 1px solid var(--ink);
    padding: 0 7px;
    line-height: 1.5;
  }
  .body { padding: 14px 12px; }
  .body p { margin: 0; }
  .ok {
    cursor: pointer;
    margin-top: 12px;
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 2px;
    color: var(--ink);
    background: transparent;
    border: 0;
    border-bottom: 1px solid var(--ink);
    padding: 0;
  }
</style>
